<!-- owner: RStack developed by Richardson Gunde -->
# Spend #283 implementation plan

1. Add red tests for measured zero, persisted/events drift provenance, measurement time, exact exhaustion, absent-cap/telemetry states, and project reconciliation.
2. Build the observation model in the state layer using the existing metrics and configured-policy contracts.
3. Serialize the same observation model to the client without recalculation.
4. Consolidate the visible Spend destination around policy, consumption, position, snapshots, and drivers while preserving Business Flex compatibility.
5. Remove the dashboard-only 80% warning threshold and label daily/monthly policy honestly.
6. Add responsive, keyboard, provenance, numeric reconciliation, and empty-state tests.
7. Run focused/full tests, lint, typecheck, validation, and local rendered QA.
8. Publish a stacked draft PR on #282 and post evidence to #283.

