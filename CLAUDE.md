# CLAUDE.md — SDLC-rstack

<!-- owner: RStack developed by Richardson Gunde -->

RStack is a governed AI-SDLC control plane: a 15-stage pipeline (00-environment → 14-cost-estimation)
with builder/validator contracts, evidence ledgers, approval gates, enforced guardrails, and the
Business Hub observability dashboard (`npm run business`, port 3008). The host framework
(Pi / Claude Code / Operator) executes the agents; RStack owns run state, contracts, and governance.

## Product goals (north star)

1. **Governed loop, enforced in code** — every guardrail, gate, and contract promise in the docs is
   backed by runtime enforcement in `src/core/harness/`, never by prompt text alone.
2. **Client-ready end-user product** — a team can install `rstack-agents`, run `init`, and get a
   working governed pipeline with observable state, without reading the source.
3. **Brownfield first-class** — adopting an existing codebase (reverse-populating stages 00–06 from
   real artifacts) is as supported as greenfield. Tracked by epic #148.
4. **Transparent state** — pipeline state is authoritative, atomic, schema-versioned, and inspectable
   from CLI (`rstack-agents pipeline status`), dashboard, and JSON — never silently degraded.

## Feature ledger — enhancements mapped to goals

Update this table whenever a PR merges. One row per shipped capability; newest first.

| Shipped | Capability | Goal | Refs |
|---------|-----------|------|------|
| 2026-07-21 | **#440 SHIPPED — Studio living room interiors: governance gate cards, evidence stacks, manager mission wall, step-inside camera** (Claude Main solo, PR #441; post-epic follow-up from Richardson's live observations, all three confirmed real): `governanceBeacon`+`vaultLight` had been DEAD PROPS (created visible=false, toggled by nothing) while `governance_items`/`evidence_items` flowed past the rooms unused — now **one floating gate card per observed governance record** (pending=rose radiating, blocked=solid red; instanced, capped 12/12, panel stays exact-count truth; beacon burns only on pending_count>0) and **sealed evidence blocks stacking beside the vault safe** (green pass / red fail / amber observed; vault light only while evidence exists; empty run = dark empty rooms). **Mission wall**: the manager faces +x but the live timeline screen hung on the north wall BEHIND him — moved to the HQ's east glass wall in his sightline, control-room sized, and upgraded to paint run goal + live-session count + pending-approvals line above the 15-stage strip (same repaint path, all projection truth). **Step inside**: room panel action glides the camera through the door to a human-height interior viewpoint — interiors relax the overview minDistance and bring their own point light, every exit path (re-frame/Overview/selection/cinema) restores both. Zero new deps — Richardson's freshly-installed animation plugin stack (A-Frame/Zdog/Framer Motion) evaluated and deliberately NOT vendored into the vanilla studio; GSAP/three.js skills used as craft reference only. Live-verified on the blocked fixture: cards visible across the floor, mission wall reading "pending approvals · 5", cinema framing governance autonomously, step-inside+exit loop, honest empty states; 0 errors on gpu=off + gpu=force. CodeRabbit: zero findings. 1541/0 | 2, 4 | #440, PR #441 |
| 2026-07-21 | **#435 SHIPPED + EPIC #431 "The Living Company" CLOSED — Studio Move D: WebGPU tier, selective bloom, transmission glass, telemetry motes** (Claude Main solo, PR #439; all four Moves A–D now shipped): **module graph chosen before load** — an inline importmap chooser gives WebGPU-capable browsers the node-based three build while everything else keeps the byte-identical classic build (the two builds cannot mix in one graph; `?gpu=off` pins classic, `?gpu=force` exercises the node pipeline on its WebGL2 backend anywhere); WebGPURenderer self-falls-back when the adapter is refused (verified live: headless Chromium exposes navigator.gpu with a NULL adapter — the trickiest production case, 0 errors); frames hold behind a ready gate (0.185 throws on pre-init render()); device.lost routes into the existing context-lost path; gpuTier in diagnostics/dataset. **MRT selective bloom**: color+emissive to separate targets, only emissive blooms — work lights/live rings/streams/gate signals radiate while text stays crisp (high tier only). **Transmission glass** (real refraction) on the node renderer. **Telemetry motes**: pure TSL vertex nodes (position=f(time,hash(instanceIndex)), NO compute pass — the skill-informed choice for decorative drift), density driven by OBSERVED live-session count (quiet floor = zero motes, iron rule), hidden under reduced motion. DoF consciously descoped (readability-first floor). **Same PR, bisected commit: two day-fresh high advisories** (js-yaml, brace-expansion) — lockfile refreshed for every patchable path; pi-coding-agent's nested brace-expansion 5.0.6 is pinned by pi's own npm-shrinkwrap.json which npm honors ABSOLUTELY (root overrides ignored inside it — proven by isolated repro; a lesson worth remembering), so it joined the audit gate's bundled-tolerance tier — which CodeRabbit then correctly tightened from package-name to **specific GHSA ids** (dormant protobufjs/ws entries dropped); CodeRabbit also caught a destroy()-races-async-init GPU leak (both fixed pre-merge). True-WebGPU-backend visual pass awaits Richardson's real-Chrome live-run test; every layer beneath it proven. 1540/0 | 2, 4 | #435 #431, PR #439 |
| 2026-07-21 | **#434 SHIPPED — Studio Move C: click-a-room inspection, Follow mode, mobile cinema window** (Claude Main solo, PR #438; third child of epic #431 — only Move D WebGPU polish remains): **click any room** (Governance, Evidence Vault, Dispatch, Library, HQ, either team wing) — the tinted floor pads carry per-instance `roomRefs` (the robots' `entityRefs` pattern) and facility props resolve via ancestor walk; camera focuses the authored anchor, a glowing selection ring drops in-world, and a **Room panel** opens assembled from the SAME validated projection collections the semantic sections render (sessions/governance_items/evidence_items/capability_attachments/missions/orchestrator) — honest empty states, zero new data plumbing, agents keep raycast priority over their room. **Follow this agent**: ride-along camera from the session inspector (offset-preserving per-frame glide so desk trips + library runs stay in frame), manual grab exits instantly, never persisted as startup default, "Stop following" action button. **Mobile**: the 3D band grew from a 220px sliver to a `min(52dvh,430px)` director-curated cinema window with the semantic Studio still primary below (all pinned #96-era contracts hold); also fixed two more hardcoded-light-background bugs (inspector + mobile semantic sheet — the Move A topbar class of bug). Agent panels +14% (Move A legibility deferral). CodeRabbit caught 2 genuine issues pre-merge: the selection ring's owned material leaked on destroy(), and the follow camera glided under prefers-reduced-motion (now steps). Live-verified headless: floor click → honest "Room · Validator Lab" panel + ring + camera dive; follow→grab→explore roundtrip; 390px mobile window. 1540/0 | 2, 4 | #434 #431, PR #438 |
| 2026-07-20 | **#433 SHIPPED — Studio Move B: Cinema camera director + a roaming orchestrator** (Claude Main solo, PR #437; second child of epic #431): **Cinema mode is now the default** — a director state machine picks shots from the SAME projection the semantic DOM renders (iron rule: never invents activity): pending `approval_summary` → frames the Governance room; else a 9s round-robin tour of ACTIVE sessions' desks; else the authored overview. Any manual intent (camera grab via `onControlsStart`, click-selection via `select()`, Overview) exits to Explore instantly; "Take control"/"Cinema mode" action button beside Overview; mode persists + survives theme rebuilds; reduced motion holds the wide shot. **Orchestrator roams**: the discovery was that walking already existed (locomotion.js synthesizes strides procedurally because the GLB cast ships no walk clips; session robots + manager check-ins already walked) — the manager just had too few allow-listed REASONS. Added two, each an observed event: `agent_session_ready` → walking check-in at the new session's desk; `agent_capabilities_attached` → NEW `manager_skill_run` to the Skills Library (dwell at shelves, return, captioned `library run · <skill>` from real skill_ids). Richardson's model-swap idea (sol_outlaw.glb / FBX example) answered honestly pre-build: that GLB ships ONE mixamo clip exactly like manager.glb — a body swap gains no movement, and FBX-vs-GLB is just a loader; movement = routes + reasons, not asset format. **Live-verified through the REAL pipeline**: appended session lifecycle events to a seeded run while the page was open (server→WS→scheduler→animator); a 100ms DOM-dataset position trace shows seated→4.5s corridor round trip to the desk (x=-14.7)→6s library run (dwell at −9.2,−9.5)→seated; desk panel showed the attached skill; the director framed the desk on its own; 0 page errors. Honest scope: procedural walking kept over clip-retargeting, sprite captions kept over CSS2D (behavior delivered either way — both noted as optional polish, not gaps). Also fixed the epic #431 body falsely pre-checking Moves C/D (the #96/#273 lesson, again). 1539/0 | 2, 4 | #433 #431, PR #437 |
| 2026-07-20 | **#432 SHIPPED — Studio "Living Company" Move A: Digital Twin theme, glass rooms everywhere, free camera, work lights** (Claude Main solo, PR #436; first child of epic #431): the Agent Force Studio's default look is now the **Digital Twin** palette (deep-navy ground, cool high-contrast light, telemetry accents) via `applyStudioTheme('twin'\|'classic')` with a topbar **Classic-look toggle** (localStorage-persisted; toggling rebuilds the scene — no live material-restore path to drift); chrome follows via `[data-studio-theme]` token overrides (the topbar had a hardcoded light bg — re-pointed). **Every interior wall is now glass** (wing dividers + both corridor walls join the Validator Lab's instanced glass; perimeter stays solid, door gaps preserved) so the whole company reads from any angle — in Classic this newly reveals the Library/HQ interiors. **Camera freed from the ground axes** — Richardson's "floor is sticked to xyz" complaint root-caused to two OrbitControls defaults: `zoomToCursor=true` (zoom heads to the pointer, not the fixed target) + `screenSpacePanning=true` (pan follows the screen at any angle), plus retuned damping/limits so zoom can't dive into the floor; verified by user-style drag-orbit+wheel-zoom in headless Chromium reaching a desk-level cinematic frame, and interactively by Richardson. **Work lights** (his "flash light on working agents" ask): capped shadowless SpotLight pool coning onto each ACTIVE session's workstation, rebuilt on reconcile beside the data streams — same honesty contract, a light exists only while its session is observed live. Discovery: the 15-stage spine's per-stage panels already paint from the real rollup (`stageSignals`) — living-spine plumbing existed; twin's emissive statuses dramatize it. CodeRabbit caught 1 genuine a11y issue pre-merge (theme button label names the NEXT action, so `aria-pressed` contradicted it — removed). Epic decisions on #431: Cinema-default, full walking, iron rule "only observed events drive the show". 1536/0, browser-regression green | 2, 4 | #432 #431, PR #436 |
| 2026-07-17 | **#400 + #401 SHIPPED — destructive-action classifier: newline-scoping false positives AND a critical path-traversal bypass, both found via a real manual Tau run** (Claude Main solo, PR #399): Richardson ran a real 8-task governed SDLC pipeline through Tau against a cloned target repo and asked for a loophole audit of the transcript. **#400**: a completely benign heredoc (writing JSON artifacts, no git involved) got blocked as git-force because `COMMAND_RULES`' `[^|;&]*` wildcard doesn't exclude newlines — a trigger phrase in prose content on one line combined with an unrelated character ~30 lines later in the same script. Not Tau-specific: the shared classifier every harness's `guard` uses had the same gap in all 8 rules that use the pattern. **#401**: `.rstack/` protected-config scope was all-or-nothing, so every task in the run needed its own `destructive-action:<taskId>` approval just to write its own routine artifacts/tasks/specs output — 8 near-identical "approve me writing my own homework" approvals, risking exactly the rubber-stamp reflex that would undermine a REAL approval prompt. Carved out `.rstack/runs/<id>/{artifacts,tasks,specs}/` while keeping every governance-critical file (manifest, tasks.json, approvals.json, decisions.json, event/evidence ledgers, checkpoints, everything outside `runs/`) protected. **CodeRabbit's review of this same PR caught a CRITICAL bug in the #401 fix before merge**: the carve-out only checked that the expected subdirectory name appeared, never that the path stopped there — `.rstack/runs/<id>/tasks/../../policy.json` satisfied the shape and was wrongly carved out, resolving straight through to a genuinely protected governance file (a real, exploitable write-protection bypass). Fixed with a traversal-rejecting lookahead, verified against forward-slash and Windows separators. CodeRabbit also caught that the #400 fix's newline-exclusion was undermined by an adjacent `\s` (which itself matches `\n`/`\r`) in 5 of the 8 patterns — fixed by narrowing those specific occurrences to `[ \t]`. Regression tests added for both original bugs and both CodeRabbit findings. Full suite 1518/0 | 1 | #400 #401, PR #399 |
| 2026-07-17 | **#402 SHIPPED — published 2.1.0 tarball was shipping ~125MB of unreferenced 3D-asset junk** (Claude Main solo, PR #403): downloaded the REAL published `rstack-agents@2.1.0` tarball from the npm registry (not just a local dry-run) while answering Richardson's "what does npm install actually ship" question, and found `src/assetsglb/` — 9 randomly-named `.glb` files, zero references anywhere in the codebase (grepped the directory path and every filename) — baked in on top of the legitimate ~35MB. Confirmed empirically that `.npmignore` has NO effect once a path is already allowlisted via `files` in package.json (a genuine npm packaging gotcha); the real fix is a negation entry directly in `files` (`"!src/assetsglb/**"`), which dropped unpacked size from 164MB to 31MB. 2.1.0's tarball is immutable — this only fixes 2.1.1+, not the version already live. Bonus: cleaned up a self-dependency (`rstack-agents` depending on itself) that had crept into package.json/package-lock.json from running setup commands directly in this checkout — `doctor`'s tripwire confirms PASS again. Regression test runs a real `npm pack --dry-run` and pins both the exclusion and a 60MB ceiling so a future large accidental addition anywhere in an allowlisted directory fails loudly. Richardson manually deleted the local `src/assetsglb/` directory (partial — 6 of 9 files remain locally, unreferenced either way, harmless since the package fix excludes the whole directory regardless of contents) | 2, 4 | #402, PR #403 |
| 2026-07-17 | **#386 SHIPPED — v2.1.0 release prep: one-page setup guide, honest per-harness docs, repo hygiene** (Claude Main solo, PR #387, continuing an earlier session's skeleton): found PR #387 already had the v2.1.0 skeleton (version bump, CHANGELOG draft, `docs/SETUP.md`, README hero image, `.pyc` removal) but it predated #388–#391 by two days, so `docs/SETUP.md`'s Tau/Operator/Hermes sections, `docs/integrations/README.md`, and the Mintlify install guide all still described the ORIGINAL WRONG mechanisms those issues fixed — Tau via the dead `tool_call` hook, Operator with no blocking hook at all, Hermes' guard returning the silently-ignored `{"decision":"block"}` shape, and no Hermes row/section at all in either doc. Merged main (resolved 4 real conflicts — both branches had independently renamed `sdlc-automation`→`sdlc-rstack`), then corrected every per-harness claim to match the shipped mechanisms (Tau's built-in shadowing, Operator's `bootstrap.py` wrapper, Hermes' real `pre_tool_call` shape + required `plugin.yaml`), added the missing Hermes coverage, and updated the CHANGELOG `[2.1.0]` entry to actually name all four harness fixes (#388–#391) instead of just Hermes. Also fixed two now-stale README claims unrelated to the harness work: a "Current limitations" bullet made obsolete by the #227 guard hook (shipped weeks earlier) and a stale 756-test badge (now 1454). CodeRabbit caught 2 more genuine issues fixed pre-merge: a `<name>` placeholder in a bash code block (shell redirection syntax, not copy-pasteable — changed to `PLUGIN_NAME`) and a Roadmap table still listing #208/#71/#228/#229 as future work when the CHANGELOG confirms all four shipped in 2.1.0 (removed the shipped rows, added the two genuinely-open items #392/#374, updated the pinned doc-validation test that hardcoded the old issue numbers). Full suite 1454/0, typecheck/lint/validate/security all clean. **Richardson holds the `v2.1.0` tag push** (triggers npm publish) — package.json is already at 2.1.0 on main, tagging is the one remaining manual step | 1, 2 | #386, PR #387 |
| 2026-07-16 | **#391 SHIPPED — the Operator adapter was entirely non-functional, now rewritten and live-verified** (Claude Main solo, PRs #398): a live audit (`pip install operator-use` 0.2.9, reading the installed source directly, then live-instantiating the rewritten plugin against it) found the previous adapter imported two modules that don't exist at all — `operator_use.extension.types` and `operator_use.tool.types` — so every import would raise `ModuleNotFoundError`; it also called `ToolResult.ok`/`.error`, methods that don't exist either (real ones are `success_result`/`error_result`). The adapter had never actually been imported by a real Operator process — worse than #389/#390's dead-hook findings, since this one couldn't even load. Rewritten against the real contract: `operator_use.plugins.Plugin` (`get_tools`/`register_hooks`) + `operator_use.tools.Tool` (decorator-wrapped handlers); `HookEvent.BEFORE_TOOL_CALL` is a REAL, genuinely blocking hook (`ctx.skip=True` + `ctx.result` short-circuits the real tool call, verified in `agent/service.py::_execute_tool`) — no built-in shadowing needed, unlike Tau. A live smoke test caught a second real bug: the real `terminal` tool's field is genuinely `cmd`, but the guard's Bash classifier reads `tool_input.command` specifically — silently allowed every destructive shell command until translated. The one genuine host-design limitation: `operator-use` has NO third-party plugin discovery at all (no entry_points, no config field, no directory scan) — `cli/start.py` hardcodes its plugin list in Python source, so `bootstrap.py` (new) monkeypatches it as a drop-in `operator` replacement, the honest maximum available; `doctor --framework operator` states this tier as a WARN, not a silent parity claim. CodeRabbit review caught 3 more genuine bugs fixed in a follow-up commit before merge: `_make_handler` misreported every bridge failure as `ToolResult.success_result` (now returns `error_result` on failure); `asyncio.wait_for` timeouts left orphaned bridge/guard subprocesses running (added a kill-and-reap helper); a malformed `RSTACK_BUSINESS_PORT` crashed plugin construction via a bare `int(...)` outside any try/except (now falls back to 3008). Two other CodeRabbit findings (config-supplied `slack_webhook` flowing into an env var; guard subprocess env not merging plugin config) were left as-is — verified identical, already-shipped patterns in the Tau/Hermes adapters, not regressions introduced here. `operator.md`/`testing-matrix.md` rewritten to the same honesty bar as `tau.md`/`hermes.md`. 1454/1 (pre-existing unrelated graphify-out failure) | 1, 2 | #391 #393, PR #398 |
| 2026-07-16 | **#390 SHIPPED — Hermes' guard was a no-op too, now fixed and live-verified** (Claude Main solo, PR #397): the same class of bug as #389's Tau finding, this time in Hermes. A live audit (real `PluginManager`, real plugin load, direct dispatch calls against `NousResearch/hermes-agent`) found `_guard_block` returned a Claude-Code-style `{"decision":"block","reason":...}` shape that belongs to a DIFFERENT Hermes subsystem (`agent/shell_hooks.py`) — the real `pre_tool_call` dispatcher (`hermes_cli/plugins.py _get_pre_tool_call_directive_details`) reads `result.get("action")` and silently ignores `"decision"`, so the guard subprocess ran on every call but never actually blocked anything, with no visible sign the block was discarded (the #375 ledger entry's claim that this shape "already emits" the right thing was wrong — corrected here). Also fixed: Hermes' real tool name (`terminal`) didn't match the guard classifier's shell/write-tool names until translated to `Bash`/`Write`/`Edit`; the real `pre_tool_call`/`post_tool_call` kwargs shapes (`tool_name`/`args`/`result`/`status`, not `tool_input`/`cwd`) didn't match what the adapter read or what `rstack-agents observe` parses; added the required `plugin.yaml` manifest (Hermes silently skips any plugin directory without one). `doctor --framework hermes` now checks the manifest and the real (not commented) `{"action":"block"}` return statement — regex-isolated to the actual `return` statement so the docstring's verbatim quote of the old wrong shape (documenting the correction) can't false-positive the check, a bug caught in this same session before merge. `hermes.md`/`testing-matrix.md` rewritten to the same honesty bar as `tau.md`. 1452/1 (pre-existing unrelated graphify-out failure) | 1, 2 | #390 #393, PR #397 |
| 2026-07-16 | **#389 SHIPPED — Tau's enforcement was a no-op, now fixed and live-verified** (Claude Main solo, PR #396): a source audit of upstream Tau (commit `4763f38`) found the adapter's `tool_call`/`before_agent_start` hooks are dead in the real engine — `AgentService._before_tool_call` never fires `ToolCallEvent` (zero instantiations anywhere in Tau's own codebase), and `tool_call` is absent from `ExtensionRuntime._INTERCEPTABLE_EVENTS` regardless, so a real Tau session enforced NOTHING despite the adapter's own docs claiming otherwise. Rewrote enforcement to shadow the built-in `write`/`terminal`/`edit` tools (real, documented Tau capability) and run `rstack-agents guard` inside each shadow's `execute()`; context injection moved to the real `input` hook. A second bug caught by the same live test: the adapter sent Bash commands as `cmd` but the guard expects `command` — would have silently allowed everything even with a live hook. Removed the `PKG_ROOT`/`BRIDGE` hardcoded-path assumption (`npx rstack-bridge`/`npx rstack-business` instead) — verified location-independent via a real tarball install into a scratch project. Packaged the adapter for `tau install` (`packaging/tau-adapter/`, generated from the single source of truth) — `pip install` verified working; `tau install` itself hits a genuine, traced upstream bug (sync CLI function calling `asyncio.create_task()` with no event loop running) documented honestly rather than claimed working. Merge with main's concurrent #371 fail-closed guard hardening reconciled by hand (kept both; `_resolve_guard_argv`'s `PKG_ROOT` fallback dropped since the adapter has no fixed install location anymore). 1451/1 (pre-existing unrelated graphify-out failure) | 1, 2 | #389 #393, PR #396 |
| 2026-07-16 | **#388 SHIPPED — the real Claude Code plugin + marketplace exist, plus a full plugins/ domain reorg** (Claude Main solo, PR #395): `plugins/sdlc-rstack/` ships 19 slash commands (one per `sdlc_*` tool + `/sdlc-resume`) driving `npx rstack-bridge`, plus the orchestrator/builder/validator team; `.claude-plugin/marketplace.json` (generated by `scripts/generate-marketplace.mjs`) makes `/plugin marketplace add` resolve the whole catalog. Reorganized 72 vendored plugins from a flat `plugins/<name>/` layout into `plugins/<domain>/plugin.json` — one consolidated manifest per domain — after Richardson's follow-up ask; declined the accompanying request to strip author/license and relabel vendored (Seth Hobson et al.) content as original, kept attribution intact instead (`THIRD-PARTY-NOTICES.md`, `contributors` arrays for mixed-author domains, SPDX `AND` for the one mixed-license domain). 9 genuinely divergent same-named specialist files renamed to disambiguate — nothing dropped. `backend-inventory.js`/`list.js`/`generate-marketplace.mjs` now discover `plugin.json` recursively, fail loud on a malformed manifest. `doctor --framework claude-code` verifies the plugin/marketplace manifests. 1275/1 (pre-existing unrelated graphify-out failure) | 1, 2, 4 | #388 #393, PR #395 |
| 2026-07-14 | **#370 destructive-classifier blind spots CLOSED + #373 budget hard-block sticks per-tool-call — enforcement lane COMPLETE** (Claude Main solo, PRs #383 #384): **#370** adds three COMMAND_RULES with false-positive carve-outs — git working-tree destruction (`checkout -- .`/`checkout .`/`-f`, `restore` unless `--staged`-only, `clean -f`; branch ops + `--dry-run` stay allowed), new `remote-exec` (`curl\|wget\|fetch … \| sh/bash/python` — plain `curl -o file` allowed), new `perm-destroy` (`chmod\|chown\|chgrp -R`; single-file `chmod 644` allowed). **#373** makes the attempt-budget hard-block stick: the guard reads task STATUS from tasks.json and denies a BLOCKED task's mutations (write tools + destructive commands + bash file-writes via the new `commandWritesFile`, scratch/`/tmp` excluded) until an audited `guardrail-override` — keyed on STATUS not attempt count (no false-block of a legit in-progress Nth attempt), NOT bypassable by `RSTACK_ALLOW_DESTRUCTIVE`, fail-safe on unresolvable run/task. Both cross-harness (shared classifier + guard). 1352/0 then 1332/0 | 1 | #370 #373 #376, PRs #383 #384 |
| 2026-07-14 | **#375 Hermes integrated via the SAME bridge as Pi/Tau/Operator (NOT MCP — Richardson's call)** (Claude Main solo, PR #382): `src/integrations/hermes/rstack_sdlc.py` is a Hermes plugin — `register(ctx)` + `ctx.register_tool` (all 18 sdlc_* tools → the Node bridge) + `ctx.register_hook("pre_tool_call", guard)`. The clean fit: Hermes normalises a Claude-Code-style `{"decision":"block","reason"}` return into a blocked call (its `agent/shell_hooks.py`) — the EXACT shape `rstack-agents guard` already emits — so the destructive gate + validator sandbox drop in with almost no glue, inheriting #371 fail-closed + #372 agent_type sandbox. Best-effort `post_tool_call`→observe + `on_session_start`→hub. Installs to `~/.hermes/plugins/rstack-sdlc/` (Hermes' own convention for 3rd-party plugins). `init --framework hermes` prints the steps; conformance test pins the 18-tool surface to the Pi registry; FRAMEWORKS contract updated. MCP server (#374) HELD | 1, 2 | #375 #376, PR #382 |
| 2026-07-14 | **#372 validator sandbox now covers Claude Code bash — read-only enforced, plugin-safe** (Claude Main solo, PR #380): the guard reads `agent_type` from the PreToolUse payload and escalates any validator-role subagent to the sandbox (one-way, beats the static `--context builder`) — the ONLY signal that works for a PLUGIN subagent, since Claude Code ignores plugin agent-def `hooks:` frontmatter (confirmed in the docs; an agent-def hook would've been a silent no-op). Second layer: the sandbox now denies file-writing bash (`echo>`, `cp`, `ln`, `tee`) while allowing reads/tests + scratch redirects (`/dev/null`, `2>&1`, `/tmp`) — with a traversal guard so `> /tmp/../src/app.js` can't escape (CodeRabbit-caught). doctor self-test proves it live; builder sandbox confirmed already at parity. Audit found budgets (attempt+telemetry) + delegate validator-stamping are ALREADY cross-harness (corrected the earlier 'Pi-only' framing). Matrix: `docs/audits/2026-07-13-cross-harness-parity-matrix.md` | 1 | #372 #376, PR #380 |
| 2026-07-14 | **#369 self-approval bypass CLOSED — the destructive gate can no longer be defeated by the agent forging its own approval** (Claude Main solo, PR #378, all-green + merged): validation showed the hole was the whole governance-state surface, not just `approvals.json`, so the fix closes the class in 3 bisected commits. **L1 classifier symmetry** — `classifyCommand` now extracts write targets (redirect/tee/cp/mv/install/dd/ln/rm/chmod/sed-i + PowerShell content cmdlets) and runs them through `classifyWritePath`, so the **bash** path matches the write-**tool** path; `PROTECTED_CONFIG_PATTERN` extended to all of `.rstack/` (`([/\\]|$)`) + the host's own guard-hook config (`.claude/settings*.json`, `*rstack-hooks.json`) so an agent can't disable its own PreToolUse guard either. **L2 opt-in HMAC provenance** (`RSTACK_APPROVAL_SIGNING_KEY`, off by default, mirrors attestations.js) — a trust-granting APPROVED record must carry a valid HMAC when keyed; signed at both writers (sdlc_approve + appendRunApproval), verified in trustedApprovedArtifacts; documented honestly as a boundary only when the key is out of the agent's reach (CI/remote/multi-user — on a same-UID host L1 is the boundary). **L3 standing red-team suite** (`tests/red-team-privilege-escalation-369.test.js`, 30 cases via the real guard CLI) so the class can't silently regress — the #265 lesson ("test the road to the gate") one level up. Cross-harness (shared classifier + one audit choke point). 1293/0 | 1 | #369 #376, PR #378 |
| 2026-07-14 | **#371 guard fails CLOSED when it cannot run — enforcement is no longer silently skippable** (Claude Main solo, PR #379, all-green + merged): the bridge/guard adapters treated any exit≠2 as allow, so a guard that failed to LOAD (partial install, version skew, cold-npx offline, exit-1 crash, timeout) silently disappeared. Now: only exit 0 = allow, exit 2 = block; **anything else = guard-unavailable → fail closed** unless `RSTACK_GUARD_FAIL_OPEN=1` (documented, safe default). Policy helper + `unavailableVerdict` in guard.js, honored by the CLI error path; **Tau adapter** rewritten to resolve a **local rstack-agents binary before falling back to `npx --yes`** (drops the per-call cold-network dependency) + subprocess timeout + loud warning on fail-open. **doctor** now reports guard resolution + whether it needs network (WARN on npx-only). Operator confirmed hookless (bridge+manual-guard tier, documented). Part of epic #376 | 1, 2 | #371 #376, PR #379 |
| 2026-07-13 | **#96 Phase 2 SHIPPED + #273 EPIC CLOSED — the Business Hub UX program is complete** (PR #360, Claude Main solo): real-browser regression layer on Phase 1 — `tests/browser/dashboard-96.test.js` driving Chromium via **playwright-core** (Richardson-directed runtime dependency so browser verification ships on install / is available to subagents; chose playwright-core over @playwright/test — dependency-free, 239 vs 481 pkgs, ZERO new audit advisories) + **axe-core** injected for a11y. Five journeys: all six #278 destinations render heading+main landmark with zero page errors; no-data readiness never renders Ready/100% in the real DOM; **axe per destination fails on serious/critical except the known color-contrast debt** (documented/tracked); responsive 1440/1024/768/390 (scope stays reachable — the audit's 'hides scope <900px' already fixed by #278, now pinned; no critical horizontal overflow; nav reachable); mobile nav opens by keyboard + closes on Escape (role=dialog). Isolated under `tests/browser/` (default `npm test` untouched at 1263/0) → `npm run test:browser`; **skips cleanly with no browser**; dedicated `browser-regression` CI job installs the headless shell (`--only-shell`), passed live in 49s. Stable ×3. **#273 epic CLOSED** — all 12 P0–P3 children done; the Hub now both observes AND (authenticated+audited) drives the governed loop | 2, 4 | #96 #273, PR #360 |
| 2026-07-13 | **#356 comprehensive July-2026 evidence refresh MERGED (supersedes #347's slice)** (PR #357, richard-devbot; Claude resolved the post-#359 conflict + merged): the broader research pass — new `research/evidence-refresh-2026-07.md` (25/25 claims verified) + METR Feb-2026 follow-up & scoped 19%, DORA 2025 baseline, **SLSA v1.2 Source Track, in-toto framework, OWASP 2025 LLM + Agentic 2026 Top 10, NIST SP 800-218A/SSDF v1.2 draft/COSAIS, EU AI Act Digital Omnibus timeline, MCP/A2A interop**. Conflicted with #359 on the 4 shared files (I'd merged #347 first); resolved by taking #357's authoritative superset wholesale (guardrails verified: null not overclaimed — 'AI does not help' in claims-not-allowed; DORA correlational; never-cite-19%). LESSON (duplicate-builder, again): #347 and #356 both refreshed `research/` — check for an existing PR before starting a docs refresh. 1263/0 | 2 | #356 #347, PR #357 |
| 2026-07-13 | **#347 evidence refresh (in-repo) SHIPPED + #79 epic CLOSED + #273 kept honest** (Claude Main solo, PR #359): research substrate no longer cites METR early-2025 / DORA 2024 unqualified — `bibliography.md`/`productivity-claims.md`/`methodology.md`/`paper-outline.md` now pair the METR RCT with the **Feb-2026 follow-up** (effect indistinguishable from zero; returning −18% CI −38%…+9%), scope the 19% figure to early-2025 tools, elevate the **perception gap** as the durable finding, and make **DORA 2025** the delivery baseline (throughput up, stability still down, correlational); binding 'never cite 19% unqualified / don't overclaim the null / DORA=correlation' rule added. Guardrails honored. **#347 stays OPEN** for item 5 only (the separate `sdlc-rstack-paper` master repo — paper.md/tex/bib — not accessible from this tree; flagged on-issue, auto-close linkage reopened). Same stretch: **#79 research-roadmap epic CLOSED** (all children #70–#78 verified closed); **#273 UX epic kept OPEN** — its body falsely pre-checked #96, corrected to unchecked; it gates on **#96 Phase 2** (browser/axe/responsive, needs a Playwright devDependency decision). 1263/0 | 2 | #347 #79 #273 #96, PR #359 |
| 2026-07-13 | **#285 authenticated, audited cockpit controls SHIPPED — the last epic-#273 child; the dashboard can now DRIVE the governed loop, not just observe it** (Claude Main solo, PR #358): threat-model-first (`docs/security/cockpit-controls-threat-model.md` — CSRF/token-leak/replay/confused-deputy/stale/traversal/concurrency/priv-esc/feature-smuggling, each mapped to an in-repo mitigation + a named test), **OFF by default / fails closed** (`RSTACK_COCKPIT_CONTROLS` env OR policy `cockpit_controls.enabled`). New pure `cockpit-actions.js` = single source of truth (flag, action catalogue/risk/audit-event names, eligibility derived from the SAME pipeline rollup the CLI reads so UI can't drift, idempotency-key validation, append-only ledger that is BOTH the idempotency store AND the immutable audit trail — append-ORDER decides in-flight, not timestamps, after a same-ms tie bug caught mid-build). Server-owned `state/cockpit.js` projection declares per-run `allowedActions` (empty when off; all disabled w/ reasons when stale). **`POST /api/action`** reuses the #238 env-write guarded chain (token + CSRF origin + rate limit + 64KB cap), resolves the target run across roots (never a body path), re-checks the flag per target root, claims the idempotency key, then: `resume-run` (low risk, no approval — the #124 model-free runner, bounded 5 steps, stops at every gate by construction; `not_eligible` when no advanceable work) and `restore-checkpoint` (high risk — the #238 two-step verbatim: deep-verify → PENDING `destructive-action:checkpoint-restore:<run>:<stage>` → MANAGER approves → one-shot consume → `rollbackToCheckpoint`, corrupt refuses). Immutable audit + run event on success; reconciles from real state (no optimistic success). UI: Run Workspace 'Cockpit controls' panel (server-declared actions only, disabled-with-reason, risk/approval badges) + accessible confirmation modal (role=dialog, aria-modal, Escape/backdrop, focused Confirm) naming canonical project·run·stage + consequence, pending→accepted→reconciled→failed states. **Browser-verified the whole loop end-to-end** (panel → modal → 409 approval_required → top bar reconciles to '1 pending' → manager approves → 202 restore → live artifact reverts V2→V1 → run event + ledger, no token leak). config-validation warns on a malformed `cockpit_controls` block. 1263/0; #75 untrusted-PR gate ran live on the PR and passed. Out of scope (per issue): start-run/start-loop split to follow-ups | 1, 2, 4 | #285 #273, PR #358 |
| 2026-07-12 | **Richardson's studio + people-layer vision SHIPPED — #352 Studio v2 + #353 email approval notifications** (Claude Main solo, PRs #354 #355; Codex at spend limit): (1) **Studio v2** — the Studio was never removed, the #278 shell had hidden it; now visible again AND showing the real governed loop: per-desk approval gates from the SAME records the claim gate enforces (pending card = GATE CLOSED red desk; APPROVED = 'gate opened by <human>'; spent override = consumed), the **'HUMANS AT THE GATES' rail** (who signed what, who holds which gate — agents build, people decide), disk-verified checkpoint markers (restore point / CORRUPT refused); browser-verified on a seeded run. (2) **#353** — manager/team_lead/developer/tester/cicd register emails in notifications.json; gate-blocked notifyAll now routes per person (exact artifact → kind wildcard → stage id → managers[] fallback) via a dependency-free **Azure Communication Services Email** channel (documented HMAC-SHA256 signing, golden-vector + fake-ACS-server tests, key env-only + hard-rejected from committable config); v1 deep-links to the Hub's audited approval card — research finding: Outlook approve-buttons can't return approver identity, so email never becomes a second gate. Provenance: started by a background agent that hit the org spend limit; salvaged from its worktree, audited, finished solo. Also: **#96 ETag settle flake root-caused + deflaked** (async index write racing back-to-back polls; paced in-loop settle) after it failed PR #354 CI once. 1238/0 | 1, 2 | #352 #353 #96, PRs #354 #355 |
| 2026-07-12 | **#283 Spend (Codex, CI-repaired) + #284 Operations Center (Claude Main) SHIPPED — Codex Waves 2 nearly closed** (PRs #348 #350 #351 + gate hotfix): (1) Codex's Spend redesign audited+merged after Claude repaired its CI — stale stacked base retargeted to main, **two live #75 gate bugs fixed** (trigger scoped to `branches: [main]` PR #350; checkout `base.ref` not the retarget-stale `base.sha` payload), and a real INDEX_VERSION collision (#283 vs #339 both claimed v8) renumbered to **v9**; Spend browser-verified honest ('policy file missing'/'No telemetry yet'/'Run cap unavailable'). (2) **#284 Operations Center**: server-owned `operations` projection (state/operations.js) — seven-section hierarchy (transport client-local reusing the ONE classifyFreshness formula + last-known-data stale banner; health computed FROM #281 inbox records so counts reconcile by construction; integrations #238+#151; recovery from `checkpoints.stages` restorable/**CORRUPT** pills + retry budgets; context/memory #136+#137+#83 drift; presence; feed demoted to secondary) — **silence is never healthy: silent producer = 'unknown'**, availability its own state; new default page of the Operations destination, legacy pages routable. Bonus fix: #280 Run Workspace `recoveryItems()` read the nonexistent `pipelineRollup.stages` — recovery panel was permanently empty; repaired onto `checkpoints.stages` (the stopped-agent contract confusion on #284 corrected on-issue). Browser-verified live; 1217/0; empty-scope isolation pinned. LESSON: verify `# fail`, not just `# pass` — 4 pinned-test failures reached CI once | 1, 2, 4 | #283 #284 #280 #75, PRs #348 #350 #351 |
| 2026-07-12 | **#71 RStack Spec v1alpha1 SHIPPED — the contracts are now a published, machine-validatable specification** (Claude Main via audited background agent, PR #349): `spec/rstack-spec.md` (normative RFC-2119, every schema/enum/gate names its source module — 'derived from the shipped code, not from intent'), primer + conformance levels (basic | business-flex | enterprise, exact MUST lists), **13 code-derived JSON schemas** (raw-file: contracts/approvals/evidence/attestations; k8s-style envelopes: Run/Task/Decision/Gate/Profile/Project/AgentRole/Adapter), `rstack-agents validate --schemas` with ZERO new deps (typebox Value.Check accepts draft-07), complete validating example run under examples/spec/ (incl. #228 stage-approval + APPROVED→CONSUMED override pair), CI step + npm tarball + Mintlify page. Audited: 1200/0 combined, 34 PASS/0 FAIL schema validation, approval enum pinned to RUN_APPROVAL_STATUSES verbatim. External teams can now conformance-test an RStack adoption | 1, 2 | #71, PR #349 |
| 2026-07-12 | **#96 Phase 1 SHIPPED — the dashboard finally has a deterministic regression suite** (Claude Main, PR #346): canonical secret-free fixtures for all 8 required states (tests/helpers/dashboard-fixtures.js — no-runs, active, blocked+override card, verified-ready, stale-snapshot, damaged, artifact matrix) + 14 checks against the real server on an ephemeral port in <0.5s: **no-data readiness NEVER reads Ready** (the flagship audit item), #276 scope isolation (3-project topology — blockers/approvals/feed never leak, per-scope ETags), artifact traversal safety, full approval token matrix (fail-closed 403 untokened), WS snapshot+reconnect (hand-rolled frame decoder, zero new deps), nav/landmark parity. **Caught a real bug on its first run**: `diagnostics.index.updatedAt` missing from the volatile ETag strip — 304 revalidation NEVER fired on multi-root setups (the #33 lesson's exact failure mode); fixed + pinned. The #75 untrusted-PR gate ran live on this PR and passed. Phase 2 (browser/axe/responsive) offered to Codex on the issue. Same stretch: #71 Spec v1alpha1 drafting via background agent | 2, 4 | #96, PR #346 |
| 2026-07-12 | **EIGHT-PR BATCH LANDED (Richardson: "merge all prs") — roadmap wave #72/#73/#74/#75/#78 + Codex Wave-1/2 #280/#281/#282, all Claude-audited on one integration branch (combined 1179/0, typecheck/lint 0, security clean)**: (1) #72 review independence — producer identity on contracts + `review_policy` (PR #338); (2) #73 DSSE-style attestation envelopes, env-only HMAC key, unknown sig types never 'verified' (PR #341); (3) #74 traceability drift scanner + CLI + `/api/drift` (read-auth-gated) + Hub drift card (PR #343); (4) #75 untrusted-PR gate — safe `pull_request_target` (base-checkout only, no installs, dependency-free evaluator) (PR #344); (5) #78 governance packs (PR #345); (6) Codex #280 Run Workspace, #281 Action Inbox, #282 Evidence Center (PRs #337/#340/#342, closed-as-contained after batch push). **Real conflict resolved in audit**: #342's Evidence Center rewrote traceability.js while #343 added a drift panel to the old page — kept the Evidence Center, ported the #74 drift card below it, test ported (21/0). Trivial append-append conflicts in bin/rstack-agents.js, src/index.js, config-validation.js resolved keep-both. Issues #72-#78, #279-#282, #156, #215 all CLOSED | 1, 2, 4 | #72 #73 #74 #75 #78 #280 #281 #282, PRs #337 #338 #340-#345, commit 562833f |
| 2026-07-12 | **#156/#215 residuals SHIPPED — the two UI-backlog issues are now fully backed by code** (Claude Main, PR #339): gap audit showed ~80% already shipped by intervening waves; the three real gaps closed: (1) per-stage checkpoint restore points with the honest **CORRUPT ≠ no-checkpoint** distinction (`checkpoint_reason` from the same deep verify → compact rollup `checkpoints.stages` → Diagnostics pills); (2) manifest `schema_version` observable (v1-legacy/v2 badge, persisted through INDEX_VERSION 8, lite↔full parity green); (3) **CONSUMED lifecycle on one-shot override cards** — server-owned `annotateApprovalLifecycle` cross-references run-level records so a spent override never reads 'approved' forever (#281 Action Inbox can consume `item.lifecycle`). Browser-verified on a live seeded governed run (which also live-verified the #228 gate cross-process: block → card → approve → claim). Joint Claude+Codex UI lane in force (Richardson-directed): coordination comments on #156/#215/#33/#273; #33 proposed close-as-superseded pending Codex ack; flagged the #273 epic body pre-checking open issues | 2, 4 | #156 #215 #33 #273, PR #339 |
| 2026-07-12 | **#228 blanket per-stage human gates SHIPPED — the top enterprise ask** (Claude Main, PR #336): `.rstack/policy.json` gains `required_stage_approvals` (canonical stage id → artifact list; any task entering the stage must see them APPROVED — no task ids needed) + `approvals.every_stage: true` (blanket `stage-approval:<stage-id>` sign-off per stage a task enters; one approval unblocks the stage for the run; a task mapping to NO canonical stage fails CLOSED on `stage-approval:<task-id>`). Pure harness module `stage-approvals.js` merged into `effectiveRequiredApprovals` — the single claim gate every claim path funnels through (build_next, #208 claim_group, runner) — enforced in EVERY mode incl. express, through the #133 audited path run-bound per #298; queue cards + paging free. `validatePolicyConfig` warns 'NEVER fire' on typo'd stage keys; sdlc_status names pending stage sign-offs. Mutation-checked (4/12 tests fail unwired). Same day: **Codex Wave-1 #279 Overview redesign Claude-audited + merged** (PR #335) — server-owned `overview` projection consuming the #93 readiness verdict (no second formula), Proof Rail across canonical stages with honest unknown-coverage labeling, esc()-disciplined render, evaluatedAt covered by the recursive VOLATILE_KEY ETag strip (the #33 lesson); combined 1102/0 | 1, 2, 4 | #228 #279, PRs #336 #335 |
| 2026-07-12 | **Board-clearing triple**: (1) #299 CLOSED for good — true `evidence_count` through the index cap (INDEX_VERSION 7) + decisions lock converged onto the #287-hardened withFileLock (Claude Main, PR #334); (2) **#229 exposure CLI verbs** — config validate / pipeline rollback / checkpoint-status / approvals audit / memory inspect, thin wrappers over already-tested harness functions (fork agent, PR #332, Claude-audited); (3) **#208 LIVE parallel-group execution** — `claim_group` prepares a whole data-independent group in one pass, ONLY when the entire fresh frontier is one gate-enabled group (the #159 evidence gate honored; #265 claim order preserved by construction — retries can never be grouped past) (Claude Office, PR #333, Claude-audited: 1087/0 combined). Also #328 continuity + Codex Wave-1 #278 shell (PR #327) merged this stretch | 1, 2, 4 | #299 #229 #208, PRs #334 #332 #333 |
| 2026-07-11 | **Session-continuity P0 SHIPPED — 'please continue' resumes the governed run** (#328, Richardson's original complaint, Claude Main): context packet becomes an INSTRUCTION for mid-flight runs (INCOMPLETE marker, stage+task coordinates, exact `pipeline run --run-id <id> --max-steps 5` command, no-restart/no-regenerate prohibitions; manifest-DONE beats stage-emptiness so express runs are never falsely ordered to resume; ≤1KB/secret-free/exit-0 preserved), orchestrator.md gains the Session Resume contract + 'please continue' trigger, and `/sdlc-resume` ships for real (skills/, human- and agent-invokable, packet↔skill command agreement pinned). Fresh-process hook shape tested via spawned CLI. Same day: audited+merged the other agents' four PRs — #221 rollup-in-index v6 (Claude Office, per the coordination briefing), #213 memory-skip observability, #203 crash-safe checkpoint restore, #241 hub nits (PRs #323/#326/#329/#330) | 1, 2, 3 | #328 #221 #213 #203 #241, PRs #331 #323 #326 #329 #330 |
| 2026-07-11 | **doctor detects pre-#286 narrow enforcement matchers** (#324, the #286 residual): installs initialized before #286 keep `Bash\|Write\|Edit` (init never overwrites) — the guard never fired for MultiEdit/NotebookEdit there and the self-test reported green (probes the binary, not the wiring). doctor's wiring check now measures matcher breadth: guard gap = FAIL naming bypassed tools + exact corrected matcher; observe gap = WARN; absent/wildcard matchers correctly pass. Existing fixtures widened; narrow case pinned + mutation-checked | 1, 2 | #324, PR #325 (Claude Main) |
| 2026-07-11 | **Validator required_checks ENFORCED — no longer advisory** (#222, the last big 'enforced in code' gap in the harness lane): new `required-checks.js` evaluates every mechanically-decidable check on the selected validator profile into real PASS/FAIL validation.json entries feeding the retry policy — builder signals reuse the validate verdicts (drift-proof), stage-artifact presence + output-contract field checks read the canonical artifact JSON (the Archon-R1 prior-art insight applied: the check list IS the stage's output schema, seeding #71), per-entry rules (every threat categorized, high risks mitigated, gaps remediated, failures root-caused), stub-signature scan; unknown check ids FAIL honestly; semantic remainder (`no_secrets_introduced`, `no_silent_skips`) stays visibly delegated (epic #72) with the delegation record naming exactly that remainder; registry-coverage test makes an evaluator-less registry check impossible to add silently; two pre-enforcement fixtures updated (they validated stage-12/07 tasks with no artifacts and passed — the #222 complaint verbatim) | 1 | #222, PR #322 (Claude Main) |
| 2026-07-11 | **Codex Wave-0 P0 pair — the dashboard stops lying about scope and policy** (Codex PRs #317/#321, Claude-audited via local test-merge + full combined suite before each merge): (1) **#276 scope trust**: canonical repository/worktree identity (filesystem-only resolution, worktree name secondary — the exact failure our own sessions hit), opaque sha256 scope keys, server-owned scoped projections with per-scope ETags behind read auth, `getIndexedRuns` dedup fixed from bare runId to source-root/run pair (the collision flagged in the alignment comment), locale/timezone timestamps with full-ISO provenance; (2) **#277 policy truth**: server-owned configured-policy reader reusing the harness's own `validateRstackConfig`/`validateBudgetConfig`, four-state availability (configured/missing/invalid/inaccessible), Business Flex + Cost & Budget show real caps before the first run — 'no cap configured' can no longer be a lie. Wave 0: #93 ✅ #276 ✅ #277 ✅; #96 remains, then Wave 1 (#278–#281) unblocks | 2, 4 | #276 #277, PRs #317 #321 |
| 2026-07-11 | **P3 papercuts roundup (7 of 9 items)** (fork agent PR #318, Claude-audited; trivial import conflict with #319 resolved by Claude on the fork branch): decisions.json fsync'd atomic; known-projects registry RMW locked+atomic in BOTH copies; worktree/submodule `.git`-as-file gitdir resolution (mis-keyed memory episodes); plan.md/prompt.md/validation.json atomic; **20-file validation cap fixed honestly** (every claimed file checked, only emitted entries capped, misses beyond cap still FAIL); retrieval events carry results_count; BLOCKED tasks surface in sdlc_status naming the exact override artifact. #299 REOPENED for item 8 (evidence-count cap) + decisions-lock convergence | 1, 4 | #299(partial), PR #318 |
| 2026-07-11 | **Validate-time exhaustion enqueues the guardrail-override card** (#274, found live in the wave verification): the FAIL→BLOCKED transition at `sdlc_validate` now creates the same pending approval-queue card the claim path produces (same `approvalQueueId`, idempotent — later claims can't duplicate it) + pages the manager; source tagged `retry_budget_exhausted` for Codex's Action Inbox (#281). Golden-path e2e redeemed its #274 marker — the card is asserted at the budget-exhaustion step of the documented journey | 1, 2 | #274, PR #319 (Claude Code) |
| 2026-07-11 | **Index blind-spot class closed for good**: (a) fork agent's v4 entries persist evidence/artifactIndex/timeline/activityTimeline/requirements + evidence.jsonl in the change signature (#296, PR #315, Claude-audited: silent-cap-at-100 filed to #299); (b) the **lite↔full parity guard** — compares a rich run fully-parsed vs index-served across EVERY key, fails on any populated field that renders empty unless consciously allowlisted with a reason — caught a real drop on its first run: the #82 'data damaged' badge (`hasIntegrityErrors`) vanished for index-served runs; persisted, INDEX_VERSION 5 (PR #316, Claude Code). A fourth occurrence of the #264/#296 class can no longer land silently | 2, 4 | #296 #274, PRs #315 #316 #319 |
| 2026-07-11 | **Five-PR batch, Richardson-directed ("validate and merge all 5"), every one Claude-audited before merge**: (1) episode store serialized on the cross-process #287 lock + atomic touch/compaction rewrites — concurrent bridge processes can no longer lose episodes (#292, PR #309); (2) **POLICY: `release-readiness.json` no longer grants a run-wide destructive bypass** — release sign-off is release-only; per-task `destructive-action:<taskId>` (or the explicit coarse `destructive-action`) is the sole unblock; regression test pins that a release approval cannot unblock `rm -rf`; docs updated post-merge (#293 decided via this merge, PR #311); (3) model escalation keys off the IN_PROGRESS task, not the globally-last task_started — parallel delegates can no longer inherit the wrong attempt count and mis-escalate cost, cross-contamination test pinned (#297, PR #313); (4) traceability.json locked + atomic + fail-closed on corrupt/odd-shaped reads (skip + loud error, file preserved) — history can never be silently wiped (#295, PR #314); (5) **Codex Wave-0 flagship: server-owned availability-aware readiness** — one projection with unknown/blocked/at_risk/ready, no-runs → unknown (never Ready/100%), Command Center + Release Readiness consume the same verdict, source-linked blockers, historical gates reconciled via the audited approval path (#93, PR #312, branch codex/ui-readiness-93) | 1, 2, 4 | #292 #293 #295 #297 #93, PRs #309 #311–#314 |
| 2026-07-10 | **Lock primitive can no longer break its own mutual exclusion**: per-acquisition token + heartbeat (mtime refreshed ~staleMs/3, token-verified so a taken-over lock is never freshened for the successor) + owner-checked release (finally deletes only OUR lock — a stale-broken owner no longer deletes the new holder's lock and admits a third writer) + `RSTACK_LOCK_STALE_MS` env tuning. Folded in the #288 residuals PR #306 deferred: `stampManifestStatus` (IN_PROGRESS/DONE stamps re-read+write under the manifest's own lock — concurrent stamps both land) and actionable `readManifest` errors naming the damaged run + recovery. Frozen-owner takeover scenario pinned in tests | 1, 4 | #287 #288, PR #307 (Claude Code) |
| 2026-07-10 | **Webhook timeout + atomic manifests** (GPT/Codex audit agent, Claude-audited+merged): `postJson` hard socket timeout (default 10s, `RSTACK_WEBHOOK_TIMEOUT_MS`, destroy-on-timeout, double-settle guard — a black-holed webhook can no longer hang sdlc_start/approve/validate; bonus: dropped `url.port` bug fixed) with a real stalled-socket regression test; `writeManifest` routed through `writeJsonAtomic` so a torn write can't brick the run (PR #306 deliberately scoped to the torn-write half; lost-update half landed with #287 above) | 1, 4 | #291 #288, PRs #305 #306 |
| 2026-07-10 | **Golden-path e2e in CI**: the documented bridge-only quick-start journey runs as a test — one real subprocess per tool call (not mock-pi; in-process mocks would hide cross-process bugs like #289), asserting version stamp (#261), persisted state + plain status (#262), run-bound approvals on the pinned run (#298/#289), FAIL-re-claim → hard-block → one-shot override (#265), structured no-task validate (#266), Hub↔terminal agreement (#264). The #261–#266 root cause ("nobody re-ran the journey") is now structurally impossible to repeat silently. #274 marker in place for the next assertion | 4, 2 | #275, PR #304 |
| 2026-07-10 | **Decision intake rejects non-canonical stages at the source**: `sdlc_decisions` + CLI `--before` refuse unknown `required_before_stage` values with a structured/actionable error (valid stage list), so a bad decision can no longer poison the fail-closed DoR gate into raw-crashing every later `sdlc_build_next`. Filed + fixed by the GPT/Codex audit agent (PR from fork), audited/approved/merged by Claude Code — review notes: legacy-poisoned stores still raw-throw (accepted or follow-up), consider validating inside `addDecision` for future callers | 1 | #290, PR #303 |
| 2026-07-10 | **Approval integrity pair — session pinning + run-bound approvals**: `.rstack/session.json` pin written by every run creator (sdlc_start AND adopt), resolution = explicit id → in-process → `RSTACK_RUN_ID` env → pin → newest dir, wired into harness `resolveRunId` and extension `readManifest` identically; `sdlc_approve` refuses no-run_id ambiguity (>1 runs, no session — structured candidate list, nothing written) and names the run every sign-off lands on; `run_id` stamped by all three approval writers + CONSUMED marker; `auditRunApprovals` threads `expectedRunId` — the #133 cross-run replay rejection is now LIVE (legacy unstamped grandfathered). Verified with a real spawned-bridge cross-process repro: pinned run receives the approval, newest run gets nothing. Post-merge incident: an over-broad `git add -A` swept 10 local workspace files into the PR — caught by the repo's own guard test, untracked same session (commit ebc2c64); lesson recorded (explicit paths only; merge only on green checks) | 1 | #289 #298, PR #302 |
| 2026-07-10 | **Tolerant JSONL readers**: one corrupt/partial line in `events.jsonl`/`evidence.jsonl` no longer breaks `pipeline status`, goal evaluation, or the dashboard rollup — per-line try/catch matching the goal-check.js precedent, evidence schema filter preserved. Filed + fixed by the GPT/Codex dogfooding agent, reviewed/merged by Claude (follow-up noted: surface skipped-line counts to the #82 integrity collector) | 4 | #294, PR #301 |
| 2026-07-10 | **Guard enforces every tool spelling + Windows grammar**: canonical tool-name comparison (separators stripped both sides — Claude Code `MultiEdit`/`NotebookEdit` PascalCase and Pi snake_case can never diverge again), `notebook_path` target extraction (5th surface found in verification), PowerShell/cmd destructive grammar (`Remove-Item -Recurse/-Force`, `rd /s`, `del /s\|/q\|/f`, content cmdlets → secret paths; case-insensitive; no false positives on single-file deletes), validator sandbox denies ANY PowerShell mutation cmdlet outright, init guard+observe matchers widened to `Bash\|Write\|Edit\|MultiEdit\|NotebookEdit`, doctor self-test probes one form per enforcement family (can never report green on an untested path again). Residual: pre-existing installs keep the old narrow matcher (init never overwrites) — doctor wiring-breadth check is the follow-up | 1 | #286, PR #300 |
| 2026-07-10 | **Dogfooding wave — six golden-path bugs fixed same day they were filed** (fresh-sandbox bridge-driven quick-start probe): (1) claim order FAIL→BLOCKED→PENDING so retry policy/attempt budgets/hard-block engage at the point of failure — the flagship #149 enforcement was unreachable mid-plan (PR #267); (2) run-level approvals survive the Hub rollup index — entry+rehydrate+signature+INDEX_VERSION 3, terminal approvals now visible for index-served runs (PR #268); (3) pipeline-state.json persisted by every state-mutating bridge tool + `pipeline status` in-memory fallback with stderr disclosure (PR #269); (4) `sdlc_validate` structured no-task response with candidates+recovery instead of a raw throw (PR #270); (5) `rstack_version` derived from package.json — drift now fails CI by construction (PR #271); (6) docs stop promising the unimplemented `.claude/agents/rstack/` local-copies feature (PR #272). Every fix mutation-checked (tests verified failing without it); live sandbox verification of the merged wave drove the full governed loop terminal↔Hub and found #274 (validate-time block never enqueues the override approval card) | 1, 2, 4 | #261–#266, PRs #267–#272; follow-ups #274 #275 |
| 2026-07-09 | **RStack status line + full hook system COMPLETE**: `rstack-agents statusline` — live governed state in the Claude Code prompt (model · stage · ✔approved/⧗pending · ◇decisions), display-only/read-only/exit-0/secret-free, top-level `statusLine` settings key, doctor check. TTS documented-not-bundled (zero new deps). **This closes the full hook system: enforcement (guard) · observability (observe) · context injection (context) · notifications (notify-hook) · quality gates (gate) · status (statusline)** — every governance-relevant Claude Code hook event covered, all exit-0-safe + secret-clean, verified by doctor on every harness | 1, 2 | #257, PR #260 |
| 2026-07-09 | **Opt-in quality-gate presets**: `rstack-agents gate plan\|tdd\|scope` — host PreToolUse hooks complementing the harness DOR/decisions. plan-gate warns (no spec), tdd-gate BLOCKS production code with no matching test (two overrides: `RSTACK_ALLOW_NO_TESTS=1` + audited `no-tests:`/`guardrail-override:` approval — never a dead-end), scope-guard warns. OFF by default; `init --gates plan,tdd,scope` wires them AFTER guard (guard stays first; no `--gates` = byte-identical default). Only guard + tdd-gate ever exit 2; everything else fails open. Adversarial review → fixed 2 false-block sources (separator-normalized test lookup; skip `__init__.py`/`conftest.py`/`setup.py`/`*.stories.*`) pre-merge | 1, 2 | #256, PR #259 |
| 2026-07-09 | **Complete governance hook-event coverage**: extended the guard/observe model to every governance-relevant Claude Code event + Tau's real surface — new `rstack-agents context` (UserPromptSubmit + SessionStart inject a ≤1KB RStack-awareness packet: run/stage + pending approvals/decisions counts + orchestrator pointer, structural-only, closes the Pi-only injection gap); `observe` extended to SubagentStart/Stop, PreCompact (`context_preserved`), PostToolUseFailure; new `rstack-agents notify-hook` (Notification → channels, 5s-race-bounded, redacted); 11-entry `CLAUDE_CODE_HOOKS`; Tau wires tool_execution_failure/before_compaction/before_agent_start (no subagent/notification events exist there — documented); doctor + full hook-map docs. Two adversarial reviews → fixed notify timeout, label/source sanitization (safeLabel + source whitelist) pre-merge. Every hook exits 0 / no-op without a run / secret-redacted | 1, 2 | #255, PR #258 |
| 2026-07-09 | **Cross-harness observability — dashboard mirrors terminal activity on every harness** (fixes Jeomon's terminal↔dashboard mismatch): root cause was that only Pi wrote tool events to `events.jsonl`; the guard on Claude Code/Tau/Operator is read-only, so the dashboard was blind. New `rstack-agents observe` (Pi-shaped events + `source` label, best-effort, ALWAYS exit 0, silent no-op with no active run, secret-redacted, lock-serialized); init wires Claude Code PostToolUse/Stop/SessionEnd; Tau uses its real `tool_result` hook (fire-and-forget); `doctor` gains an observability check; dashboard labels harness sources. **Hotfix #254**: closed secret-redaction leaks (`DB_PASSWORD=`/`API_TOKEN=`/`AWS_SECRET_ACCESS_KEY=`/`Bearer` were leaking — inline `\b` bug), made Tau observe non-blocking, capped stdin. Two adversarial reviews (2nd caught the leaks post-merge → hotfix). | 2, 4 | #251 #253, PRs #252 #254 |
| 2026-07-07 | **Dead-easy cross-harness setup**: `rstack-agents doctor [--framework] [--json]` — env + config + per-framework wiring checks with exact per-FAIL fixes, a **live guard self-test** (blocks `rm -rf`, allows `ls` → proves enforcement is live on the machine), hub health, and a self-dependency tripwire (npm-i-inside-repo); `docs/integrations/testing-matrix.md` scratch-dir-first recipes per framework. Verified e2e: scratch `init --framework tau` → `doctor` = 10 PASS/0 FAIL | 2 | #244, PR #247 |
| 2026-07-07 | **Generic bridge + Tau adapter + conformance contract**: `bin/rstack-bridge.ts` (framework-neutral, `RSTACK_BRIDGE_CALLER`, `--list`), operator-bridge → back-compat alias; `src/integrations/tau/rstack_sdlc.py` (Jeomon George's contribution, credited) shelling to the bridge + guard `tool_call` hook; `init --framework tau`; **both Python adapters synced to the full 18-tool Pi surface** (closed the Operator 15→18 gap); `docs/integrations/adapter-contract.md` + `tests/bridge-conformance.test.js` pin every adapter to the Pi registry. Framework story: native (Pi) \| bridge+hook (Operator, Tau) \| guard-hook (Claude Code) \| guided (rest) | 2 | #243 #246, PR #248 |
| 2026-07-07 | **Static type-check gate + Pi SDK 0.79.x drift fix**: first real `tsc` gate (`typescript`+`@types/node`+`tsconfig.json`, NodeNext/loose, `npm run typecheck` in prepublish + CI); fixed all 13 drift errors — the `pi.tools`-removed migration to a local `registerTool` capture map (behavior-preserving, LLM registration unchanged), `AgentToolResult.details` required, `@types/node` for ChildProcess, index-type narrowing; mock-pi updated to real 0.79.x API + command-path regression test so drift fails CI next time. **The editor "red lines" are now real CI signal.** | 1, 4 | #242, PR #249 |
| 2026-07-07 | **Hub Environment & Integrations page + approval-gated `.env` writes** (2.1 wave): `src/core/harness/env-file.js` (parse/update preserving comments+quotes, `listEnvKeys` returns names+lengths only — never values, `isEnvGitignored` via git check-ignore→.gitignore fallback→fail-closed); `POST /api/env-write` two-step gate (classify secret-write → PENDING queue approval `destructive-action:env-write:<KEY>` + 409, value persisted nowhere → approve on Approvals page → resubmit → consume-then-write one-shot, crash-safe → env-writes-audit.jsonl key/actor/length only + `env_key_written` event); `POST /api/decide` resolve/waive decisions from the Hub; new "Environment" page (Operate); managers + enforce_in_express documented as live gates (closes #225 doc residual). Two independent adversarial reviews: 0 blocking (nits → #241) | 2, 1 | #238, PR #240 |
| 2026-07-07 | **Interactive environment intake** (2.1 wave): `rstack-agents env scan [--json]` reuses adopt scanner detectors (zero duplication) → proposes run mode + evidence + `setup_needs[]`; environment_report v2 with `validateEnvironmentReport` (legacy warn-only, run_mode enum-enforced, credential-keys rejected) wired WARN-by-construction into stage-00 validate; `.rstack/integrations.json` (endpoints only, secret-named keys = error → .env) registered in CONFIG_FILES + init template; 00-environment detect-first intake raises setup needs as Decision-Queue items gated on the CONSUMING stage (ticketing→05, deploy→09, notify→10); 05-jira reads integrations.json first | 3, 2 | #237, PR #239 |
| 2026-07-07 | **v2.0.0 release prep**: version 2.0.0 (package + lock), CHANGELOG [2.0.0] "governed loop enforced in code" entry (Unreleased → v2.1 planning, pinned tests updated), `assets/` with logo + interactive THREE.js 3D workspace + rendered preview, README "Meet the studio" section (clickable preview → live raw.githack view; absolute image URLs so npm renders them), Stephens citation added to research bibliography + stale snapshot stamps refreshed. **Awaiting: Richardson pushes the v2.0.0 tag → publish.yml → npm** | 2 | #235, PR #236 |
| 2026-07-07 | **Universal enforcement guard**: `rstack-agents guard` — framework-neutral gate any host hook can call (stdin Claude Code PreToolUse JSON or flags; exit 0 allow / exit 2 block), reusing the harness classifier + validator sandbox + #133 audited per-task approvals (zero duplicated logic); `RSTACK_VALIDATOR_CONTEXT=1` beats `--context builder` (no flag escape), destructive-with-unresolvable-task fails CLOSED, raw text sniffed as bash before the fail-open path; `init --framework claude-code` installs the PreToolUse hook idempotently; `docs/integrations/wire-your-own-harness.md` paste-in prompt for codex/gemini/custom. **Claude Code is now enforced, not template-only.** Adversarial review: 0 findings | 1, 2 | #227, PR #234 |
| 2026-07-07 | **Docs truth & discovery**: real counts everywhere (68 skills — a stray untracked `skills/logs` dir had inflated local counts; 723 tests; 196 agents per validate), complete README CLI table (14 commands + 2 bins), "Govern an existing codebase" section, roadmap rewritten shipped-vs-future, "any framework" reworded to verified enforcement tiers, mintlify (61 files) + loop-recipes ship in tarball (10.1→6.6MB), new `reference/pipeline.mdx` | 2 | #223, PR #233 |
| 2026-07-07 | **Governance enforcement closeout**: destructive-action gate wired into the live Pi `tool_call` hook (centralized classifier + audited `destructive-action:<taskId>` approvals, fails closed, blocked-event ledger write failures logged not swallowed); context-pressure classified at prompt-assembly (`phase:"pre_execution"`) before model spend; honest validator-profile delegation record naming the owning specialist + delegated required_checks (first slice of #222 — real PASS/FAIL evaluation stays open, semantic = #72) | 1 | #210 #212 #222(partial), PR #230 |
| 2026-07-07 | **Agent-prompt / harness sync**: builder.md documents the cost/context/execution/routing telemetry blocks (routing honestly marked recorded-not-extracted) + the destructive-approval NEEDS_CONTEXT path; validator.md points at registry profiles as guidance (enforcement = #222/#72); 11-feedback-loop over-stamp rejection rule + canonical paths | 1, 4 | #226, PR #232 |
| 2026-07-07 | **Repo hygiene**: `specs/` → `docs/internal-specs/` (history-following, still unshipped); untracked `identity.md` → `docs/audits/`, stale `outputs/`/`logs/`/`skills/logs` archived out of tree | 4 | #224, PR #231 |
| 2026-07-06 | **Context pressure warnings**: detect-only classifier (`context-pressure.js`) with configurable thresholds (builder prompt / memory block / artifact + stage summaries / token ratio) validated per #151, pinned `context_pressure_warning` event (`source` field; emits ONLY what the code actually does — no `memory_pruned` claim without pruning), best-effort at validate (a throw can never fail validation), `context_pressure` rollup in pipeline-state + status CLI; closes BLE-6 — **epics #130 + #134 both closed: backend loop-engineering program (BLE-1→6) fully shipped** | 4 | #136, PR #211 (pre-execution wiring → #212) |
| 2026-07-06 | **Destructive-action classifier**: centralized `classifyDestructiveAction` (broad-delete, git-force, publish, deploy, secret-write, protected-config-write, db-destroy incl. ORM/CLI forms) — single in-repo source of truth for builder + validator contexts, obfuscation-tested (env-prefix, /bin/rm, --force-with-lease…), no false positives on safe commands; `evaluateDestructiveAction` requires an audited `destructive-action:<taskId>` approval via the #133 path (cross-run replay rejected). Validator sandbox keeps its stricter deny-outright policy (documented divergence) | 1 | #131, PR #209 (enforcement wiring + sandbox convergence → #210) |
| 2026-07-06 | **Parallel-execution benchmark**: `bench-parallel.mjs` + `parallel-benchmark.js` — SEQ vs PAR timing for data-independent stage groups (default 12/13/14), evidence gate `parallel_groups.enabled` only at ≥40% measured improvement (fails safe to disabled on any bad input), real data-independence detection, 6-stage cap rejects loudly, honest mock-vs-real labeling in the run artifact the Hub indexes; runner stays sequential — execution wiring is #208 | 2, 4 | #159, PR #207 |
| 2026-07-06 | **Memory write policy enforced in code**: `evaluateWritePolicy` is the single write decision — `appendEpisode` overwrites caller `trusted` flags (launder-via-flag defended), non-PASS episodes skipped under `validator-approved-only` / written `trusted:false` under `validation-attempts`, PASS-trust gated on signature + evidence + quality-score integrity, retracted/untrusted episodes never reach the prompt, `episode_memory_skipped_untrusted` event | 1, 4 | #137, PR #206 (observability nit → #213) |
| 2026-07-06 | **Goal gate fails closed on unreadable state**: corrupt/unreadable `events.jsonl` on a recipe-driven run now returns `goal_activity_indeterminate` FAIL instead of silently disarming the stage-11 gate; goal-activity permanence documented | 1, 4 | #200, PR #205 |
| 2026-07-06 | **Cost/token telemetry persisted** (P0): builder-contract `cost`/`context`/`execution` extracted at validate into `metrics.json` — `cumulative_tokens {input,output,total}`, per-stage `stage_cost_usd`/`stage_tokens`, `cumulative_tool_calls`; increments are idempotency-keyed on builder-contract content hash (retries/loop iterations can't double-count, so the loop budget cap enforces on real spend), `metrics_write_failed` drift event with event-recompute fallback, mid-run upgrade seeds from history, read path prefers persisted totals; docs schema in HARNESS.md | 4 | #83 #135, PR #199 (adversarial review: F1 blocking + 3 fixed pre-merge) |
| 2026-07-06 | **Approval audit consistency**: approval records are a trust boundary — `validateApprovalRecord`/`auditRunApprovals`/`trustedApprovedArtifacts` audit before trusting (safe run/artifact, exact-casing status, actor, timestamp, dashboard token evidence, run binding, replay + append-only ordering); malformed **latest** record poisons its artifact (no fallback), both unblock gates (guardrail-override + required-approval) unified on ONE audit path, `approval_audit_failed` events, fail-loud write boundary | 1 | #133, PR #202 (review: replay-drift MEDIUM fixed pre-merge) |
| 2026-07-06 | **Critical-stage checkpoints**: pre/post restore points for 06/07/08/09/12 (configurable), sha-256 integrity manifests (corrupt checkpoint → `CORRUPT`, fails closed — no best-effort lies), restorability verified on disk, wired into claim/validate/rollback, composes with BLE-4 loop stage resets (reset changes task status, never checkpoint state) | 1 | #132, PR #201 (hardening → #203) |
| 2026-07-06 | **Goal contract runtime-enforced**: `validateStageGoalEvaluation` wired into `sdlc_validate` — a goal-driven run FAILS validation (into validation.json + the retry policy) when agent-11's `goal_evaluation` is missing/malformed; no enforcement without an active goal; stage targeting reuses the rollup's `taskStageIds` so gate and loop reset can't disagree | 1 | #196, PR #198 (review MERGE; corruption edge → #200) |
| 2026-07-06 | **Agent-11 goal contract**: `goal_evaluation` in feedback.json feeds the goal evaluator as an evidenced judge-verdict writer — explicit human/host verdicts always consumed first (id-less shorthand included), agent stamps ahead of the current iteration rejected, evidence must resolve to a real file inside runDir/projectRoot (existence-not-relevance documented honestly), conflicting duplicate criteria consumed by neither, agent rerun recommendations UNION the recipe's stages so the loop self-sustains; closes BLE-4 — **all six BLE epics done** | 1 | #128 #126, PR #195 (adversarial review: 5 findings fixed pre-merge, runtime wiring → #196) |
| 2026-07-06 | **BLE-4 goal loop (core)**: model-free goal evaluator (`goal-check.js` — verifiable criteria evaluated by the harness; judge criteria close via iteration-stamped `goal-verdict.json`, unstamped = stale in loop context), bounded budget-capped `rstack-agents pipeline loop` (default 3 iterations, hard cap 20 no config can exceed, no-progress stop, budget checked pre-iteration), in-lock stage resets that can't launder attempt budgets, five pinned loop events in status/feed, `docs/loop-recipes.md` (recipes tagged with maintenance taxonomy) | 1 | #127 #129, PR #193 |
| 2026-07-06 | **Adopt-aware agents**: run-modes contract (greenfield/brownfield/feature) in OPERATING-STANDARD §8 + SOUL, "Adopted-Run Behavior" in ALL 15 stage agents (detection recipe with `RUN_BASE` fallback, refine-never-regenerate, study-before-modify per Stephens Ch11 p243), markers verified against `harvest.js`; completes the #148 flagship end-to-end | 3 | #183, PRs #192 #189 #190 #191 |
| 2026-07-06 | **Stephens book alignment**: 02-requirements Ch4 quality gates (clear/unambiguous/consistent/prioritized/verifiable, words-to-avoid, FURPS+, five Ws, MoSCoW, changes via Decision Queue); 08-testing five-level taxonomy + black/white/gray-box; 09-deployment deliberate cutover strategy (`cutover` block required) + canonical-then-legacy write path; 10-summary defect analysis from real BLE-3 events + Ishikawa grouping + honest nulls; 11-feedback-loop maintenance taxonomy (perfective/adaptive/corrective/preventive) + bug-swarm rule | 1, 4 | #184 #185 #186 #187, PRs #189 #190 #191 |
| 2026-07-05 | **Brownfield adoption** `rstack-agents adopt`: read-only scanner + evidence-or-skip harvesters for all 15 stages, dry-run plan writes nothing, adoption run is DONE-with-evidence + resumable, specialist gap scan, migration guide packaged; closes flagship epic #148 + #160 | 3 | #148 #160, PR #181 |
| 2026-07-05 | State-of-RStack audit doc: four-discipline cross-verification, trigger×goal loop framework check, Stephens book grounding, full pending map + onboarding for any coding model | 2 | PR #180 |
| 2026-07-05 | Resume-aware runner `rstack-agents pipeline run`: skips DONE work, validates active contracts, re-claims retryable failures via the model-free bridge, stops at every human gate; pure planner shared with `--dry-run` (persists nothing); closes BLE-3 epic #121 | 3, 4 | #124, PR #178 |
| 2026-07-05 | Retry event trace: `{scheduled, exhausted, human_required}` rollup counts, per-stage `retry_state`, refined status-CLI recommendations, attempt-counter trace lines, feed rendering of `task_retry_*` events | 4 | #125, PR #177 |
| 2026-07-05 | Deterministic retry policy: `retry_recommendation` × attempt budgets → atomic in-lock task transitions (FAIL/BLOCKED/NEEDS_CONTEXT) with pinned `retry_decision` event contract | 1 | #123, PR #176 |
| 2026-07-05 | Validator sandbox: validator/reviewer/security contexts hard-blocked from writes/destructive shell/publish/secret paths via Pi tool_call hook (no override path), env-stamped by sdlc_delegate, read-only tool defaults; closes BLE-2 epic #117 | 1 | #119, PR #174 |
| 2026-07-05 | Validator registry: stage-specific validator profiles for 06/07/08/12/13 with priority selection (security first), `.rstack/validators/registry.json` overrides (read_only unclampable), profile recorded in validation.json | 1 | #120, PR #173 |
| 2026-07-05 | "RStack in 5 Minutes" quick-start: bare-terminal tour via the bridge, approval gate as the hero moment; packaged in npm, linked from README + Mintlify | 2 | #158, PR #171 |
| 2026-07-05 | Reference SDLC agents (00/06/07/08/11) normalized to canonical stage paths + Task Contract sections; 07-code plan-task/stage-id conflation fixed; closes BLE-1 epic #112 | 4 | #116, PR #170 |
| 2026-07-04 | Dashboard read-path auth: RSTACK_DASHBOARD_READ_TOKEN(_FILE) gates state/artifact/run-report + WS; foreign Origins always rejected on reads and WS upgrades; tokens session-scoped | 2 | #164, PR #168 (external audit) |
| 2026-07-04 | Release hygiene: evidence ledger lock-serialized, publish workflow gains lint + security-audit parity with CI, README counts honest | 1, 4 | #165 #166, PR #167 (external audit) |
| 2026-07-04 | Loop-engineering UI slice: feed names blocked task + reason, override one-shot explainer on approval cards, Guardrail-blocked signal in Needs Attention, retry-event rendering slot | 2 | #156 (partial), PR #162 |
| 2026-07-04 | Hub hardening: TLS opt-in (fails loudly if half-configured), token-file rotation without restart, timing-safe compare, signing-key fallback warning | 2 | #150, PR #161 |
| 2026-07-04 | Config validation on load: field-level warnings for all `.rstack/*.json`, surfaced in Diagnostics + at hub startup | 4 | #151 |
| 2026-07-04 | Data-integrity collector: corrupt run files recorded per run, Diagnostics panel + "data damaged" run badges | 4 | #82 |
| 2026-07-04 | Schema migration registry (`migrations.js`); manifests stamped `schema_version: 2`, legacy runs migrate on read | 4 | #82 |
| 2026-07-03 | Completeness gate hardened: junk evidence (`[{}]`) rejected, non-array option shapes tolerated | 1 | #154 follow-up |
| 2026-07-03 | Builder contract completeness as shared harness API (`validateBuilderCompleteness`); Pi delegates | 1 | #118, PR #154 |
| 2026-07-03 | `rstack-agents pipeline status` CLI (text + `--json` + `--regenerate`); run-id resolution lifted to `runs.js`; `RSTACK_STATE_DIR` mismatch fixed | 4 | #115, PR #153 |
| 2026-07-03 | Runtime guardrail enforcement: attempt budgets hard-block at claim (task → BLOCKED), one-shot `guardrail-override:<task_id>` approvals (crash-safe, consumed in-lock), telemetry budgets at validate, per-project budgets in `rstack.config.json` | 1 | #149, PR #152 |
| 2026-06-21 | Pipeline state rollup (`pipeline-state.json`, atomic, schema_version 1) | 4 | #113, PR #146 |
| 2026-06-17 | Dashboard freshness indicator; security deps sweep; artifact viewer | 2, 4 | PRs #139, #142, #107 |

Pre-2026-06 history lives in CHANGELOG.md (v1.0 → v1.9.0-rc).

## Next steps queue (live — reorder as priorities change)

Work top-down. File a GitHub issue before any branch (Richardson's rule: issues before PRs).

**Backend closeout COMPLETE (2026-07-06):** waves 1+2 shipped — #196, #132, #133, #83, #135,
#137, #159, #131, #136, #200 (PRs #198/#201/#202/#199/#206/#207/#209/#211/#205). Epics #130
(BLE-5) and #134 (BLE-6) closed; the backend loop-engineering program (BLE-1→6) is fully shipped.

**v2.0.0 SHIPPED to npm (2026-07-07):** waves A+B+C merged (PRs #230-#234, #236); Richardson
pushed the v2.0.0 tag; `npm view rstack-agents` = 2.0.0. #225 closed INVALID — `managers` +
`enforce_in_express` are live gates, do not re-file. Framework story is "enforced on Pi,
Operator, Claude Code (guard hook); guided self-wiring elsewhere".

**2.1 wave 1 COMPLETE (2026-07-07):** onboard & operate — PRs #239 (#237 environment intake) +
#240 (#238 Hub Environment page, approval-gated .env writes) merged.

**2.1 wave 2 COMPLETE (2026-07-07):** cross-harness / dead-easy setup — PRs #249 (#242 typecheck
gate + Pi drift), #248 (#243 generic bridge + Tau adapter + conformance, closed #246), #247
(#244 `doctor` + testing-matrix) merged. Main green: **833 tests**, typecheck 0 errors. Verified
e2e: scratch `init --framework tau` → `doctor --framework tau` = 10 PASS/0 FAIL (adapter +
bridge + live guard self-test all PASS). #241 (hub nits) still open. NOTE: two duplicate
builders occurred this wave (one #243, one #242) — resolved by keeping the superset branch and
discarding the other; watch for this.

**2026-07-10 triage (dogfooding wave merged; full-corpus audit #287–#299 + Codex UX epic #273
open).** Two agents active: Claude (harness/governance) + GPT/Codex (dashboard UX program
#273/#276–#285 + robustness audit #287–#299). Alignment comments posted on every issue.

~~1. Approval-integrity pair #289+#298~~ SHIPPED (PR #302). ~~2. #275 golden-path e2e~~
SHIPPED (PR #304). ~~#290~~ SHIPPED by the GPT/Codex agent (PR #303, Claude-audited).
Multi-agent protocol in force: CLAIM an issue with a signed comment before starting;
Claude Code posts from richard-devbot, the audit/Codex agent from richardsongunde.

~~Durability core #287/#288/#291~~ ALL SHIPPED (PRs #305/#306 by the fork agent, #307 by
Claude Code — board-sync comment on #287 records the division).

~~#292/#293/#295/#297 + #93~~ ALL SHIPPED 2026-07-11 (five-PR batch #309/#311–#314,
Richardson-directed merge, each Claude-audited). #293 policy DECIDED by that merge:
release-readiness.json is release-only; docs fixed same day.

~~#274~~ SHIPPED (PR #319). ~~#296~~ SHIPPED (fork agent PR #315 + Claude parity/integrity
follow-up PR #316).

~~#222~~ SHIPPED (PR #322, Claude Main). Lanes in force: Claude Main = harness; Claude
Office = #221; Codex = UX epic (#96 → Wave 1).

1. **Fork agent's lane**: #299 residuals (item 8 evidence-count cap + decisions-lock
   convergence). Claude audits + merges. Goal 4.
~~#286 residual~~ SHIPPED as #324 (PR #325).
~~Session-continuity P0~~ SHIPPED as #328 (PR #331). ~~#221/#213/#203/#241~~ SHIPPED by the
other agents (PRs #323/#326/#329/#330, Claude-audited+merged).
   NOTE (Richardson 2026-07-11): release 2.1.0 prep is PARKED until all bugs/issues are closed
   and everything is verified working. ~~#299~~ ~~#229~~ ~~#208~~ ~~#278~~ ~~#228~~ ~~#279~~
   ALL SHIPPED (2026-07-12). ZERO fix-type items remain. ~~#156~~ ~~#215~~ ~~#72-#75~~
   ~~#78~~ ~~#280-#282~~ ALL SHIPPED+CLOSED (PR #339 + the eight-PR batch, 2026-07-12).
   ~~#71~~ ~~#283~~ ~~#284~~ ~~#352~~ ~~#353~~ SHIPPED (PRs #349 #348 #351 #354 #355).
   ~~#285 secured controls~~ SHIPPED 2026-07-13 (PR #358, browser-verified, 1263/0) — last
   #273 P3 child. ~~#347 evidence refresh (in-repo)~~ SHIPPED (PR #359). ~~#79 epic~~ CLOSED
   (all children shipped).
   ~~#96 Phase 2~~ SHIPPED (PR #360, playwright-core+axe, browser-regression CI job green).
   ~~#273 UX epic~~ CLOSED (all 12 P0–P3 children done). ~~#79~~ CLOSED.
   NEW WAVE 2026-07-14 (harness-parity epic **#376**, from the feasibility audit): security +
   cross-harness hardening. SHIPPED+MERGED: ~~#369~~ self-approval (PR #378), ~~#371~~ guard
   fail-closed (PR #379), ~~#372~~ validator sandbox on Claude Code bash via agent_type + bash-write
   denial + traversal fix (PR #380), ~~#375~~ **Hermes via the SAME bridge as Pi/Tau** (PR #382,
   NOT MCP — Richardson's call), + CodeRabbit follow-ups (PR #381: #379 doc contradiction, #378
   test timeout). **PARITY DONE: Pi/Tau/Claude Code/Hermes all enforce the full governed loop.**
   REMAINING backend items on #376: ~~#370~~ SHIPPED (PR #383), ~~#373~~ SHIPPED (PR #384).
   **ENFORCEMENT LANE COMPLETE** — the ONLY remaining backend item is **#374 MCP server (P2, HELD
   per Richardson, folded into next release)**. Parity matrix:
   `docs/audits/2026-07-13-cross-harness-parity-matrix.md` — **now stale on Operator** (says
   "bridge+manual-guard tier, no blocking hook"; #391 gave Operator a real blocking hook via
   `bootstrap.py`, update the matrix doc next time it's touched). Budgets (attempt+telemetry)
   confirmed cross-harness via sdlc_build_next/sdlc_validate.
   OPEN BOARD (older tail): **#33** (close as superseded — pending Richardson's ack); **#347
   tail** = item 5 only (apply the METR-2026/DORA-2025 fixes in the separate `sdlc-rstack-paper`
   repo — not reachable from this tree).
   ~~release 2.1.0 prep~~ SHIPPED as #386 (PR #387, 2026-07-17) — version bump, CHANGELOG, SETUP.md,
   README hero, hygiene all done. **Only the tag push remains, held for Richardson.**
   NEW EPIC 2026-07-16 (**#393**, distribution & end-to-end onboarding — Pi-parity for every
   harness, the sibling to #376's enforcement-parity work): ~~#388~~ Claude Code plugin +
   marketplace SHIPPED (PR #395) — also reorganized `plugins/` into domain-consolidated manifests
   per Richardson's follow-up ask (declined the accompanying request to strip vendored authors'
   attribution — kept it, added THIRD-PARTY-NOTICES.md instead). ~~#389~~ Tau Pi-parity SHIPPED
   (PR #396) — found and fixed a LIVE bug: Tau's `tool_call`/`before_agent_start` hooks are dead
   upstream, so Tau enforcement had been a no-op this whole time; fixed via built-in shadowing +
   the real `input` hook, live-verified against the real `tau-coding-agent` package. ~~#390~~ Hermes
   live-verify SHIPPED (PR #397) — found the SAME class of bug as #389: `_guard_block`'s
   `{"decision":"block"}` shape belonged to a different Hermes subsystem and was silently ignored by
   the real `pre_tool_call` dispatcher, so the guard never actually blocked anything; fixed the
   shape + tool-name translation + both hooks' kwargs shapes + added the required `plugin.yaml`
   manifest, live-verified against a real `hermes-agent` install. ~~#391~~ Operator live-verify
   SHIPPED (PR #398) — found the WORST of the three: the adapter imported two modules that don't
   exist at all (`operator_use.extension.types`, `operator_use.tool.types`), so it had never even
   loaded successfully; rewrote against the real `Plugin`/`Tool`/`BEFORE_TOOL_CALL` contract (a
   genuinely blocking hook — stronger than Tau/Hermes needed), fixed a live `cmd`→`command`
   field-translation bug, shipped `bootstrap.py` as the honest-maximum wiring since `operator-use`
   has zero third-party plugin discovery, and fixed 3 more real bugs CodeRabbit caught pre-merge
   (bridge-failure misreported as success, orphaned subprocess on timeout, malformed-port crash).
   **All four #393 fix items (#388–#391) are now SHIPPED.** REMAINING: **#392** (end-to-end
   live-test matrix per harness — epic's own suggested next step, since only Pi is exercised via
   automated CI today). Richardson explicitly asked to HOLD #392 until he can manually test the
   matrix himself — do not start it without his go-ahead. Directives 2026-07-13: Claude solo, NO
   subagents per Richardson; backend logic stays in place.
   ~~#386~~ SHIPPED 2026-07-17 (PR #387) — **release 2.1.0 prep is now COMPLETE**: version bumped
   to 2.1.0, CHANGELOG `[2.1.0]` covers the full wave including #388–#391, `docs/SETUP.md` +
   README hero + `docs/integrations/README.md` + Mintlify install guide all corrected to match
   the shipped (not the originally-assumed) Tau/Operator/Hermes mechanisms, repo hygiene (tracked
   `.pyc` removed, `.gitignore` hardened). **ONE MANUAL STEP REMAINS: Richardson pushes the
   `v2.1.0` git tag** to trigger the npm publish workflow — held deliberately, not automated.
   ~~#400~~ ~~#401~~ ~~#402~~ SHIPPED 2026-07-17 (PRs #399 #403) — found via Richardson's own
   manual Tau loophole-hunt: destructive-action classifier newline false positives + a CRITICAL
   path-traversal write-protection bypass (CodeRabbit-caught, fixed pre-merge) + the published
   2.1.0 tarball shipping ~125MB of unreferenced junk. **A 2.1.1 patch release is now warranted**
   on top of the already-complete 2.1.0 prep — same held-for-Richardson tag/publish step applies.
   Still open, lower priority: the 6 missing `LICENSE.txt` files found under `skills/` (flagged,
   not yet restored — pending the skills domain-reorg conversation) and the skills/ domain
   reorg itself (Richardson asked for it, not yet started — `skills/` is already partially
   bucketed: 12 domain dirs holding 145 nested skills + 57 flat leaf skills to sort).
   TRANSPARENCY NOTE: epic bodies pre-check children as [x] before they ship (bit us on #96 under
   #273) — verify child STATE via `gh issue view`, never trust the epic checkboxes.
   DEP NOTE: playwright-core + axe-core are runtime `dependencies` (Richardson-directed, so browser
   verification ships on install for subagents); browsers install separately via
   `npx playwright-core install --only-shell chromium`. If a lighter published tree is wanted later,
   moving them to devDependencies is a one-line change.
4. **Session-continuity P0** (from the 2026-07-10 audit: imperative context packet +
   orchestrator Session Resume + real /sdlc-resume skill) — then the standalone-executor RFC.
   Goals 2, 3.
5. **Codex UX epic #273** proceeds in parallel on the dashboard (Wave 0: #93 ✅ #276 ✅
   #277 ✅; #96 remains, then Wave 1 #278–#281). Claude coordination notes live on each
   issue. Goal 2.
6. **Release 2.1.0 prep** — the wave is getting complete: version bump + CHANGELOG (must note
   the #293 governance tightening as breaking) + tag. Goal 2.
7. Prior queue (still valid, after the above): #222 remainder, #228, #229, #208, #203, #213,
    #241; Release 2.1.0 when the wave feels complete; #156 remainder; #71 spec; #90–#97 UI
    backlog (largely superseded by #273); research epics #72–#79.

UI ↔ backend alignment note (2026-07-04 review): the dashboard approve path writes run-level
`approvals.json` (`appendRunApproval`), so guardrail overrides approved from the Business Hub reach
the claim gate end-to-end — the remaining UI work in #156 is presentational, not plumbing.

External audit reconciliation (identity.md, 2026-07-04): all 5 confirmed-new findings fixed same
day (#164–#166 + README + localStorage); remainder were already fixed by this session's merges,
already tracked (BLE phases, #73, #83, #159), or false alarms (.DS_Store untracked). Strategic
read: hardening is now DONE for the current stage — reputation comes from adoption, so #158
(quick-start) and #148 (brownfield adopt) outrank further internal robustness work.

Declined / out of scope (do not re-open without new context):
- Runtime tool-call interception in the harness — host frameworks execute tools; strongest available
  enforcement is validate-time telemetry checks + host-side hooks (#131 tracks host classification).

## Maintenance protocol (for Claude)

- **After every merged PR**: add a Feature-ledger row (date, capability, goal number, refs) and
  remove/reorder the Next-steps entry it closes. Keep both lists honest — this file is the
  single at-a-glance answer to "where are we and what's next".
- **After every merged PR (memory graph)**: sync the MAIN working tree to main (`git pull` at
  /Users/richardsongunde/projects/SDLC-rstack — it goes stale; a stale tree caused the identity.md
  audit drift) and re-index: `codebase-memory-mcp cli index_repository
  '{"repo_path": "/Users/richardsongunde/projects/SDLC-rstack"}'`. When an architecture decision
  changes, update the stored ADRs via `manage_adr`. Graph UI: http://localhost:9749/.
- **After every review cycle**: record genuinely declined findings under "Declined / out of scope"
  with the rationale, so future sessions don't re-litigate them.
- **At ship time**: CHANGELOG.md gets the user-facing entry (branch-scoped version bump per the
  workspace rules); this file tracks the engineering view. Don't duplicate CHANGELOG content here.
- **Verification gates before any PR**: `npm test`, `npm run lint`, `npm run validate`,
  `node scripts/security-audit.mjs`, `git diff --check`. All must pass.
- **Commit style**: bisected — one logical change per commit; mechanical refactors separate from
  features; tests land with the change they verify.
- Deeper technical reference: `docs/HARNESS.md` (run state, contracts, guardrails),
  `agents/OPERATING-STANDARD.md` (agent behavior), `docs/LOOP-ENGINEERING-UPGRADE-PLAN.md` (BLE roadmap).
